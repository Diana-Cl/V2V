# نام Worker شما در داشبورد کلادفلر
name = "rapid-scene-1da6"

# فایل اصلی کد شما
main = "worker.js"

# تاریخ سازگاری برای استفاده از قابلیت‌های جدید
compatibility_date = "2024-03-20"

# فلگ‌های سازگاری (برای فعال کردن تست پینگ TCP)
compatibility_flags = ["nodejs_compat"]

