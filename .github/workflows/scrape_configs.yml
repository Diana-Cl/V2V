name: Scheduled Scrape and Build

on:
  schedule:
    - cron: "0 */4 * * *" # کاهش اجرا به هر ۱۲ ساعت برای جلوگیری از مشکل سهمیه گیت‌هاب
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-commit:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Create dummy download test file
        run: |
          # یک فایل ۱۰۰ کیلوبایتی برای تست سرعت دانلود ایجاد می‌کند
          dd if=/dev/zero of=dl-test.bin bs=100k count=1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests PyYAML PyGithub

      - name: Run scraper script
        run: python scraper.py
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        timeout-minutes: 40

      - name: Commit and Push final files to GitHub
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          # اضافه کردن تمام فایل‌های جدید به کامیت
          git add ./configs/*
          git add cache_version.txt
          git add dl-test.bin
          
          if git diff --staged --quiet; then
            echo "No new changes to commit."
          else
            git commit -m "📊 Update configs [$(date -u '+%Y-%m-%d %H:%M:%S UTC')] [skip ci]"
            git push
          fi
      
      - name: Purge Cloudflare Cache
        if: success()
        run: |
          # با استفاده از ورکر، به جای پاک کردن کش، یک سیگنال آپدیت ارسال می‌کنیم
          # این روش نیازی به Zone ID ندارد
          curl -X POST \
          -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
          "https://api.cloudflare.com/client/v4/accounts/${{ secrets.CLOUDFLARE_ACCOUNT_ID }}/workers/scripts/${{ secrets.CLOUDFLARE_WORKER_NAME }}/edge-cache" \
          -d '{"enabled":false}'
          
          sleep 5 # Wait for the cache to be disabled

          curl -X POST \
          -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
          "https://api.cloudflare.com/client/v4/accounts/${{ secrets.CLOUDFLARE_ACCOUNT_ID }}/workers/scripts/${{ secrets.CLOUDFLARE_WORKER_NAME }}/edge-cache" \
          -d '{"enabled":true}'
