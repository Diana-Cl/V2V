name: 'V2V Config Updater'

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]
    paths: 
      - 'scraper.py'
      - 'sources.json'

jobs:
  update_configs:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        pip install requests PyGithub PyYAML

    - name: Run Scraper
      run: |
        timeout 2700 python scraper.py || echo "Scraper completed with errors"
      env:
        GH_PAT: ${{ secrets.GH_PAT }}
        GITHUB_SEARCH_LIMIT: 150

    - name: Commit Changes
      run: |
        git config user.name "GitHub Action"
        git config user.email "action@github.com"
        git add all_live_configs.json clash_subscription.yml cache_version.txt
        git diff --quiet && git diff --staged --quiet || git commit -m "Update configs $(date -u '+%Y-%m-%d %H:%M')"
        git push --force

  deploy_vercel:
    needs: update_configs
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Deploy to Vercel
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        vercel-args: '--prod --force'
      continue-on-error: true

  deploy_arvan:
    needs: update_configs
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        ref: main

    - name: Deploy to Arvan
      uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --delete --exclude ".git/*" --exclude ".github/*" --exclude "*.py" --cache-control "no-cache,must-revalidate"
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.ARVAN_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.ARVAN_SECRET_ACCESS_KEY }}
        AWS_S3_ENDPOINT: 'https://s3.ir-thr-at1.arvanstorage.ir'
        AWS_S3_BUCKET: ${{ secrets.ARVAN_BUCKET_NAME }}
        SOURCE_DIR: '.'
      continue-on-error: true

  deploy_pages:
    needs: update_configs
    runs-on: ubuntu-latest
    if: always()
    
    permissions:
      contents: read
      pages: write
      id-token: write
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        ref: main

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Upload
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'

    - name: Deploy
      id: deployment
      uses: actions/deploy-pages@v4