# اصلاح شده: مرحله 🔧 Execute Enhanced Scraper
    - name: 🔧 Execute Enhanced Scraper
      run: |
        echo "🚀 Starting enhanced V2V scraper..."
        
        # Run scraper with comprehensive timeout and error handling
        # 2700 seconds = 45 minutes
        timeout 2700 python scraper.py 2>&1 | tee scraper.log || {
        # ... (بقیه کدها بدون تغییر)
      env:
        GH_PAT: ${{ secrets.GH_PAT }}
        GITHUB_SEARCH_LIMIT: 150
        MAX_CONFIGS_TO_TEST: 8000
        MAX_TEST_WORKERS: 80
        V2V_LOG_LEVEL: INFO # 💡 تعیین سطح لاگ‌گیری به INFO (یا WARNING)
      timeout-minutes: 47
